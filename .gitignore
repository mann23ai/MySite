# Local tooling and notes — deliberately kept out of the public repo.
# These files stay in your working folder; they just aren't published.

README.md
LAUNCH.md
publish.sh

# Transfer archives dropped into this folder — never publish these.
*.zip
_to_delete/

.DS_Store
_to_delete*
_backup-prev/
